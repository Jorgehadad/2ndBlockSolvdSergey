package com.solvd;

import jakarta.activation.DataSource;

public class MainRunner {
    public static void main(String[] args) {
        DataSource dataSource;
        
        /*
        ICarService carService = ServiceFactory.buildCarService(dataSource);
        carService.add(car);
        IGarageService garageService = ServiceFactory.buildGarageService(dataSource);
        garageService.getAllCars();
        */
    }
    
}
